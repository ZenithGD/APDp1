#pragma once

#include <vector>
#include <map>

using namespace std;

/**
 * @brief Program that implements the algothim of Radix Sort 
 * 
 * @param arr Vector that we want order
 */
void radix_sort(vector<int> &arr);

/**
 * @brief Program that implements the algothim of Quick Sort 
 * 
 * @param arr Vector that we want order
 */
void quick_sort(vector<int> &arr);

/**
 * @brief Program that implements the algothim of Count Sort 
 * 
 * @param arr Vector that we want order
 */
void count_sort(vector<int> &arr);